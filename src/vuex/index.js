import { install, Store } from './store';
import { mapState, mapGetters, mapMutations, mapActions } from './helpers';

export default {
  install,
  Store,
  mapState,
  mapGetters,
  mapMutations,
  mapActions,
}

export {
  install,
  Store,
  mapState,
  mapGetters,
  mapMutations,
  mapActions,
}